Master node
- API server (everything goes through here)
- controller-manager (replication, namespace, serviceaccounts)
- scheduler
-> etcd (data is persisted)

Worker node
- kubelet
- kube-proxy
- pod (containers)
- pod (containers)

Deployments
- replica set(s) -> pod(s)

inside container, try running
- curl localhost:8080

inside container, try running
- curl [service_name]:8080


