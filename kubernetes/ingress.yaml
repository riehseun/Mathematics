apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: basic-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "jenkins-master"
spec:
  backend:
    serviceName: web
    servicePort: 8080